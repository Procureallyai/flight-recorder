# Flight Recorder Validation Matrix

| Surface | Required evidence | Current status |
|---|---|---|
| Canonical documents | Required files exist and truth validator passes | Verified locally on 19 July 2026 after the documentation and licensing remediation. A final integrated rerun remains required after public-history reconciliation. |
| Supplied handoff preservation | Repository copies match supplied SHA-256 hashes | Verified |
| Deterministic passport | Valid bundle verifies; changed bundle fails | Verified locally on 18 July 2026 after independent security review and remediation: strict signed-envelope schema, deterministic seal policy, signed five-call review provenance, signed human finding decisions, Ed25519 key enforcement, event chain, Merkle root, artifacts, manifest tamper, artifact tamper, stale-review, duplicate-review, accepted-risk, algorithm confusion, prototype-name, and symbolic-link cases pass focused checks. A 21-file portable directory bundle exports and independently verifies; missing, changed, unexpected, oversized, case-colliding, and symbolic-link inputs fail safely. The generated fixture is synthetic and is not final provenance. |
| Codex capture | Genuine observable session captured or supported fallback imported | Verified for fallback Gate B: the final desktop-bundled `codex-cli 0.145.0-alpha.18` run used `gpt-5.6-terra`, made one evidenced audit-response remediation, passed 16 tests, and imported 15 sanitised observable events with zero issues. Finalisation added a post-commit passing test and final Git-state envelope, producing 17 bound events and four artifacts at commit `8fd7a35`. Raw reasoning was discarded; identifiers and local paths were pseudonymised; approval coverage is correctly `not-observed`. |
| Genuine passport candidate | Genuine events and final artifacts assemble without premature seal claims | Verified locally on 19 July 2026: remediated review binding assembled 17 genuine events, four committed artifacts, 19 findings, and five review receipts. The unsealed candidate remained blocked until Floyd's explicit approval. The resulting judge passport contains an 18th typed approval event, 15 resolved decisions, four accepted-risk decisions, and an Ed25519 signature generated without persisting the private key. Independent verification passes all eight checks, and artifact tampering fails. |
| GPT-5.6 runtime | Real schema-valid review with evidence references | Verified for Gate C: four specialist calls and one synthesis call completed with `gpt-5.6-sol`, strict Structured Outputs, `store: false`, bounded output, no tools, and evidence references. All specialists returned `pass`; synthesis returned `ready`. Four open low-severity scope warnings remain preserved for human decision. |
| Local application | Tested on explicitly claimed platform | Partial on macOS: command-line session initialisation validates an allowed Git worktree root, records baseline commit and dirty state, requires explicit policy acknowledgement, requires `.flight-recorder/` to be ignored by Git, writes permission-restricted private state, and rejects symbolic-link storage. The bridge binds to `127.0.0.1`, enforces one-time authenticated pairing and strict loopback origins, and exposes only status-level capability. Live preflight, App Server initialisation, ephemeral thread creation, and a bounded no-command turn match desktop-bundled `codex-cli 0.145.0-alpha.18`. The genuine `codex exec --json` fallback capture is verified separately. A complete operator interface and live approval exchange remain unverified. |
| Hosted judge route | Public, no-login flow completes twice | Verified on 19 July 2026 at `https://flight-recorder-web.vercel.app/`: two fresh `in-app browser proof` runs each completed `VERIFIED` to `INVALID` after a safe memory-only artifact change and returned to `VERIFIED` after restoration. Both runs had a clean browser console. A fresh unauthenticated Hypertext Transfer Protocol request returned status 200 with the configured security headers. Profile 7 trusted browser proof reports Vercel production status `Ready` from inspected deployment-source commit `0329093` on the Hobby plan. |
| Security | Secret scan, threat model, archive safety, and redaction tests | Partial after local remediation: nine scanner regression tests pass, 161 public files pass the strengthened privacy scan without printing candidate values, and no tracked working-tree file contains the private feedback identifier. Vite is upgraded to 6.4.3, and both production and full dependency audits report no known vulnerabilities. Earlier application hardening and hosted security-header checks remain valid. Public-history rewriting is complete locally; publication and fresh remote-ref privacy verification remain open. |
| Quality | Formatting, lint, type check, unit, integration, end-to-end, and build gates pass | Verified locally after remediation: the production build, 126 product tests, 16 demonstration tests, nine scanner regression tests, 161-file public privacy scan, genuine passport's eight verification checks, production and full dependency audits, submission-metadata parsing, canonical-truth validation, and diff checks pass. Rewritten-current-head Continuous Integration remains open until publication. |
| GitHub | Correct remote, visibility, licence, release commit, tag, and checks | Verified before remediation: `https://github.com/Procureallyai/flight-recorder` is public, reports Apache License 2.0, and uses `main` as the default branch. The public annotated `v0.1.0` tag peels to release commit `9c51da56a133a689187c0b6347f5fd5b639e3cb4`, and the GitHub Release is non-draft and non-prerelease. Current-head Continuous Integration run `29681862636` passed commit `f4f4902b6b02136a70fc28c1c415c6fa05ca9fec`. GitHub release immutability is not enabled. The remediated history, tag, release, and current-head checks require fresh live verification after publication. |
| Video and screenshots | Final release shown, public YouTube under three minutes, no sensitive data | Verified: two final 1440 by 960 pixel screenshots are committed and attached to the Devpost draft. The accepted 163-second H.264 and Advanced Audio Coding film uses five screen-aligned Cedar scenes generated by OpenAI's `gpt-4o-mini-tts` speech model. It passes codec, dimensions, duration, loudness, true-peak, metadata, sampled-frame, and silence checks, visibly discloses synthetic narration, and contains no private key, feedback identifier, billing view, bookmark bar, or local path. Floyd accepted it, Profile 7 trusted browser proof published it publicly at `https://youtu.be/kGo3LUvXG98`, and an unauthenticated request reached a status-200 watch page. A separate 1440 by 960 pixel Devpost thumbnail is prepared; authenticated upload remains open. |
| Devpost | Profile 7 draft saved at final human-only review | Partial: Profile 7 trusted browser proof and the Devpost connector confirm that the project name, tagline, story, four accepted technology tags, judge and repository links, public YouTube video, two gallery screenshots, Individual submitter type, United Kingdom residence, Developer Tools category, judge instructions, installation and test instructions, and private feedback field are saved. The submission is four of five steps complete and remains unsubmitted. The prepared 3:2 project thumbnail is not yet uploaded. Final legal acceptance and submission remain human-only. |
| Human acceptance | Entrant, authority, ownership, eligibility, terms, and final submit | Partial: Floyd accepted the product direction, narrow seal claim, four demonstration-scope warnings, hosted experience, and final video. Eligibility, ownership representations, acceptance of terms, and final submission have not been performed. |

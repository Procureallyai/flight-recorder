# Flight Recorder Validation Matrix

| Surface | Required evidence | Current status |
|---|---|---|
| Canonical documents | Required files exist and truth validator passes | Verified locally on 18 July 2026: delivery skill quick validation and `scripts/validate-canonical-truth.sh` both pass. |
| Supplied handoff preservation | Repository copies match supplied SHA-256 hashes | Verified |
| Deterministic passport | Valid bundle verifies; changed bundle fails | Verified locally on 18 July 2026 after independent security review and remediation: strict signed-envelope schema, deterministic seal policy, signed five-call review provenance, signed human finding decisions, Ed25519 key enforcement, event chain, Merkle root, artifacts, manifest tamper, artifact tamper, stale-review, duplicate-review, accepted-risk, algorithm confusion, prototype-name, and symbolic-link cases pass focused checks. A 21-file portable directory bundle exports and independently verifies; missing, changed, unexpected, oversized, case-colliding, and symbolic-link inputs fail safely. The generated fixture is synthetic and is not final provenance. |
| Codex capture | Genuine observable session captured or supported fallback imported | Verified for fallback Gate B on 18 July 2026: desktop-bundled `codex-cli 0.145.0-alpha.18` completed a genuine synthetic remediation run with exit code 0; 19 sanitised observable events imported with zero issues; raw reasoning was discarded; identifiers and local paths were pseudonymised; approval coverage is correctly `not-observed`; 6 remediation tests pass. Test classification recognises the documented frameworks, permits configured and manual confirmation, rejects package-manager build and lint commands, and derives pass or fail from exit code rather than output text. The implemented App Server client also completed live initialisation, ephemeral thread creation, and a bounded no-command turn. The hardened normaliser retained four sanitised task and completion events with zero issues and no command or file-change evidence. Approval mapping is fixture-tested; live approval exchange remains unverified. |
| Genuine passport candidate | Genuine events and final artifacts assemble without premature seal claims | Verified locally on 18 July 2026: `pnpm candidate:generate` assembled 19 genuine events and 2 covered artifacts from repository commit `75c0e45`; the candidate passes strict schema construction and records a stable event-chain head and Merkle root. It remains unsealed with zero findings, `ready: false`, and `humanApproved: false` until genuine GPT-5.6 review and explicit human approval. |
| GPT-5.6 runtime | Real schema-valid review with evidence references | Verified for Gate C: four specialist calls and one synthesis call completed with `gpt-5.6-sol`, strict Structured Outputs, `store: false`, bounded output, no tools, and evidence references. The hardened genuine rerun returned `not-ready` with 26 findings; a post-remediation rerun remains required before sealing. |
| Local application | Tested on explicitly claimed platform | Partial on macOS: command-line session initialisation validates an allowed Git worktree root, records baseline commit and dirty state, requires explicit policy acknowledgement, requires `.flight-recorder/` to be ignored by Git, writes permission-restricted private state, and rejects symbolic-link storage. The bridge binds to `127.0.0.1`, enforces one-time authenticated pairing and strict loopback origins, and exposes only status-level capability. Live preflight, App Server initialisation, ephemeral thread creation, and a bounded no-command turn match desktop-bundled `codex-cli 0.145.0-alpha.18`. The genuine `codex exec --json` fallback capture is verified separately. A complete operator interface and live approval exchange remain unverified. |
| Hosted judge route | Public, no-login flow completes twice | Local interface implemented only: the no-login verification split view, proof disclosure, deterministic verified-to-invalid in-memory tamper transition, restore action, loading state, and mobile layout were exercised through in-app browser proof on 19 July 2026. Public deployment and two independent hosted runs remain unverified. |
| Security | Secret scan, threat model, archive safety, and redaction tests | Partial: strict-envelope, evidence-bound seal-gate, Ed25519 algorithm, named-secret, inline-credential, output-bound, malformed-key, traversal-path, prototype-name, symbolic-link, reserved-path, case-fold collision, event-count, artifact-size, private-session, authenticated-loopback, App Server malformed-message, reasoning-exclusion, approval-correlation, and bundle-inventory tests pass. The public-file secret scan passed across 123 tracked files on 19 July 2026 without printing values, and the full dependency audit reported no known vulnerabilities. Threat-model assumptions await human confirmation; public hosted-route validation remains. Archive ingestion is not a supported surface. |
| Quality | Formatting, lint, type check, unit, integration, end-to-end, and build gates pass | Partial: the web production build passes and the remediated demonstration workspace passes 10 deterministic tests covering exact expiry, repeated and concurrent redemption, rollback after reset-action failure, neutral responses, complete safe telemetry, and dependency failures. In-app browser proof covers verified, verifying, invalid, restore, proof-details, and 390-pixel mobile states with no console warnings, console errors, or horizontal overflow. Full product tests still require the narrow elevated loopback route, and public hosted end-to-end checks remain open. |
| GitHub | Correct remote, visibility, licence, release commit, tag, and checks | Unverified |
| Video and screenshots | Final release shown, public YouTube under three minutes, no sensitive data | Not started |
| Devpost | Profile 7 draft saved at final human-only review | Partial: overview saved; release-dependent fields remain |
| Human acceptance | Entrant, authority, ownership, eligibility, terms, and final submit | Not performed |
